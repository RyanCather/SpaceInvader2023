# Player.gd

```mermaid
flowchart TD
    terminalStart([Start])
    %% Comment
    terminalEnd([End])

```

# Global.gd

```mermaid
flowchart TD
    terminalStart([Start])
    %% Comment
    terminalEnd([End])

```